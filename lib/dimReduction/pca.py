"""

This file runs pca on an roidb passed into the fxn call

We ran run on RAW of CROPPED (reqs. bboxes) images

We optionally save the pca version of the images to file.

We return the pca images to the caller

"""
